import 'package:flutter/material.dart';

class TampilanReviewPage extends StatelessWidget {
  const TampilanReviewPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}

import 'package:flutter/material.dart';

class CreateUpdateProdukPage extends StatelessWidget {
  const CreateUpdateProdukPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
